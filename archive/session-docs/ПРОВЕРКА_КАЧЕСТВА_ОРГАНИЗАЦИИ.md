# Проверка Качества PE-Based Organization

**Цель**: Verify реальное распределение vs оптимальное
**Подход**: Critical assessment + system-level improvements

---

## ✅ ЧТО СРАБОТАЛО ХОРОШО

**Automatic migration правильно определило**:

1. **Problematic code → Project #2**:
   - purification_test.py (weak methodology) ✓
   - formal_purification_proof.py (circular) ✓
   - MAXIMUM_SCALE_FINAL.py (LD data) ✓
   - hypergraph_engine.py (infrastructure) ✓

2. **Historical → archive**:
   - All ИТОГ*/ФИНАЛ* files (11 files) ✓
   - Session summaries ✓

3. **Overclaimed → Project #2/resources**:
   - manuscript.tex (5 theorems version) ✓
   - THEOREM_COMPLETE (overclaimed) ✓

---

## ⚠️ ЧТО МОЖНО УЛУЧШИТЬ

### Issue #1: Routing Logic Too Simple

**Current logic**:
```bash
if grep "purification" → Project #2
```

**Problem**: Doesn't distinguish между:
- purification_test.py (problematic, needs fixing)
- purification axiom (concept, valid for both)

**Better logic needed**:
```python
def classify_artifact(file):
    content = read(file)
    
    # Semantic analysis, not keyword match
    if has_logical_flaw(content):
        return "project2/needs-fixing/"
    elif has_overclaim(content):
        return "project2/overclaimed/"
    elif is_solid_supporting(content):
        return "paper1/"
    elif is_infrastructure(content):
        return "shared/" # Both can use!
    else:
        return "archive/"
```

### Issue #2: Shared Infrastructure Not Recognized

**hypergraph_engine.py**: Хороший код, нужен ОБОИМ проектам!
- Paper #1: Для воспроизводимости κ=0.67
- Project #2: Для новых экспериментов

**Current**: Moved to Project #2 only
**Better**: Shared library или duplicate

### Issue #3: Data Separation Incomplete

**Data files**: Не все перемещены в data/
- spatial_critical_results.txt (66KB) - где?
- MAXIMUM_SCALE_FINAL_RESULTS.json - где?
- Много JSON в output/ не тронуты

**Need**: Systematic data migration

---

## 🎯 SYSTEM-LEVEL IMPROVEMENTS

### Для PE/CW Mechanisms (Upstream Fix)

**Problem**: `/cws-route` не существует как явный skill

**Должно быть**:
```yaml
# pe-route.md skill (TO CREATE)
name: pe-route
category: organization
description: |
  Intelligent artifact routing based on:
  - Content analysis (not just keywords)
  - Logical flaws detection
  - Scope alignment (VOS-based)
  - Value track assignment

usage: |
  /pe-route <file> --target-project <name>
  # Analyzes content
  # Checks for: overclaims, logical flaws, solid vs speculative
  # Routes to appropriate location
  # Updates VALUE-TRACKS.yaml automatically
```

**Should integrate**:
- Semantic content analysis (not just grep)
- VOS scope-checking
- Automatic value track updates
- Reference updating

### Для `/pe-classify` Enhancement

**Current**: Classifies by L0/L1/L2/L3 (abstraction levels)

**Should also classify by**:
- Quality: solid/speculative/flawed
- Scope: deliverable/research/deprecated
- Status: ready/needs-work/archived

**Output**: Multi-dimensional classification

```yaml
artifact: purification_test.py
classifications:
  level: L2 (project-specific)
  quality: flawed (weak methodology)
  scope: research (needs fixing)
  status: needs-rewrite
  route_to: project2/src/needs-fixing/
```

### Для Dynamic Reorganization

**Should exist**: `/cws-reorg` skill

```yaml
# Triggered when:
- Scope changes (split projects)
- Quality assessment changes (peer review)
- Value tracks evolve

# Actions:
- Re-classify all artifacts
- Route to new locations
- Update cross-references
- Preserve git history
```

---

## 📋 КОНКРЕТНЫЕ ДОРАБОТКИ ДЛЯ CW СИСТЕМЫ

### 1. Create `/pe-route` Skill

**Location**: `$CW_ROOT/foundation/universal/skills/pe-route.md`

**Capabilities**:
- Semantic content analysis
- Logical flaw detection
- Scope alignment checking
- Automatic routing decision

### 2. Enhance `/pe-classify`

**Add dimensions**:
- quality (solid/speculative/flawed/deprecated)
- scope (deliverable/research/infrastructure/historical)
- readiness (ready/needs-work/needs-major-revision)

### 3. Create `/cws-reorg` Skill

**Triggers**:
- Scope change detected (VOS update)
- Quality change (review feedback)
- Project split/merge

**Actions**:
- Automatic re-classification
- Routing execution
- Reference updates

### 4. VALUE-TRACKS.yaml Integration

**PE should**:
- Auto-create on project init
- Auto-update on artifact changes
- Auto-route based on track assignments

---

## 🎯 HOW TO UPDATE SYSTEM

### Via ClaudeWorkspace

```bash
# From CW root
cd $CW_ROOT

# Create new skill
cat > foundation/universal/skills/pe-route.md << 'SKILL'
# /pe-route - Intelligent Artifact Routing

## Purpose
Semantic artifact routing based on content analysis, not just keywords

## Usage
/pe-route <artifact> --analyze

## Algorithm
1. Read artifact content
2. Detect: logical flaws, overclaims, solid claims
3. Check scope alignment (VOS)
4. Determine quality level
5. Route to appropriate location
6. Update VALUE-TRACKS.yaml
SKILL

# Distribute update
/cws-distribute  # Push to all nodes
```

### Via Pull Request (If Applicable)

If this is improvements to PE itself:
- Create PR to PE repository
- Document use case (this project!)
- Show before/after (messy vs clean)

---

## ✅ ИТОГ ПРОВЕРКИ

### Текущая Организация: 7/10

**Good**:
- Separation exists (Paper #1 vs #2)
- Critical materials moved
- VALUE-TRACKS created

**Needs improvement**:
- Shared infrastructure not recognized
- Data files not fully migrated
- Routing too keyword-based

### С System-Level Fixes: 9/10

**Would add**:
- Semantic routing (/pe-route skill)
- Multi-dimensional classification
- Automatic reorganization on scope change
- Shared infrastructure handling

---

## 🚀 IMMEDIATE ACTION

**For THIS project**: Manual fixes (30min)
- Move data files properly
- Handle shared infrastructure
- Complete migration

**For CW SYSTEM**: Create improvement proposal
- `/pe-route` skill specification
- Enhanced `/pe-classify`
- `/cws-reorg` skill
- Submit for integration

---

**Делаю оба параллельно?**

1. Fix current organization (complete local)
2. Create system improvements (for CW upstream)
